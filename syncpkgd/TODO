important:
- log the exceptions
- handle SIGTERM just like SIGINT

it would be nice to have:
- mail the m8r if failed
  - check if mail addresses are all in authors.xml
- cron job to clean up old build logs (server)
- properly stop the build on client SIGINT
- store the last sent pkg to a client
