- build and upload the package, nost just simulate it
  - and log exit code, etc (based on syncpkg)
- feedback from the client to the daemon (exit code, log)
  - mail the m8r if failed
    - check if mail addresses are all in authors.xml
- git hook to add builds
- web interface
- cron job to clean up old build logs
- properly stop the build on client SIGINT
